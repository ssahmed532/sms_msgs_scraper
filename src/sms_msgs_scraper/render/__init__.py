"""Output renderers. Table, JSON and CSV are peers, not a default and two extras.

`console_ui.py` holds what they share: the two consoles, the theme, and the cell
helpers that keep data-derived strings from being read as console markup.
"""
